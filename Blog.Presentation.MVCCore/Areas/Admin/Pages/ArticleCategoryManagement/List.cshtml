@page
@model Blog.Presentation.MVCCore.Areas.Admin.Pages.ArticleCategoryManagement.ListModel

<table class="table table-sm table-bordered text-center">
    <tr>
        <th>Id</th>
        <th>Title</th>
        <th>Status</th>
        <th>CreationDate <a asp-page="Create" class="badge badge-warning">New Category</a></th>
        <th>Commands</th>
    </tr>
    @foreach (var articleCategory in Model.ArticleCategories)
    {
        <tr>
            <td>@articleCategory.Id</td>
            <td>@articleCategory.Title</td>
            <td>

                @if (articleCategory.IsDeleted)
                {
                    <i class="fa fa-close text-danger"></i>
                }
                else
                {
                    <i class="fa fa-check text-success"></i>
                }
            </td>
            <td>@articleCategory.CreationDate</td>
            <td>
                <a href="Edit/@articleCategory.Id" class="mr-2 mt-1 badge badge-info float-left"><i class="fa fa-edit"></i></a>
                @if (articleCategory.IsDeleted)
                {
                    <form class="float-left" method="post" asp-page-handler="Activate">
                        <button type="submit" class="border-0 badge badge-success"><i class="fa fa-magic"></i></button>
                        <input type="hidden" name="id" value="@articleCategory.Id" />
                    </form>
                }
                else
                {
                    <form class="float-left" method="post" asp-page-handler="Delete">
                        <button type="submit" class="border-0 badge badge-danger"><i class="fa fa-trash"></i></button>
                        <input type="hidden" name="id" value="@articleCategory.Id" />
                    </form>
                }
                @* <a href="#" class="badge badge-danger"><i class="fa fa-trash"></i></a> *@
            </td>
        </tr>
            

            
    }
</table>